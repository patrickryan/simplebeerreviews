extends layout

block content
  h4= title
  p All beers reviews in one place.
  div
  form(method='post', action='/api/beer_lookup')
    input(type='text', name='beerName' placeholder='Search for beer')
    input(type="submit" value="Find Beer" class="button submit-btn")
  
